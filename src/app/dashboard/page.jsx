import React from "react";
import styles from "./dashboard.module.css";
const page = () => {
  return (
    <section className={styles.section}>
      <h1>Workign ...</h1>
    </section>
  );
};

export default page;
